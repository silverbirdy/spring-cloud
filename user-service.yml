spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: '{cipher}encryption_password' # config-server 에서 /encrypt raw test를 입력한 값

token:
  expiration_time: 86400000
  secret: user_token
